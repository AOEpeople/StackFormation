sudo: false
language: php
php:
  - 5.5
  - 5.6
  - 7.0
branches:
  except:
    - /^(\d+\.)+\d+$/
before_script:
  #- curl -OL https://squizlabs.github.io/PHP_CodeSniffer/phpcs.phar
  - composer install
script:
  # Code Style
  #- php phpcs.phar --standard=./phpcs.xml --encoding=utf-8 --report-width=180 ./src
  - phpunit --debug
notifications:
  email:
    recipients: [ travisci@fabrizio-branca.de ]
    on_success: always
    on_failure: always
